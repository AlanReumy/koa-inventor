{
"name": "<%=(projectName) %>",
  "version": "1.0.1",
  "description": "a koa server",
  "main": "index.js",
  "type": "module",
  "scripts": {
  "start":"node index.js",
  "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
  "koa": "^2.13.4"
  <% if (middleware.includes('koa-router')) { %>
    ,"koa-router": "^12.0.0"
    <% } %>
      <% if (middleware.includes('koa-static')) { %>
        ,"koa-static": "^5.0.0"
        <% } %>
          <% if (middleware.includes('koa-bodyparser')) { %>
            ,"koa-bodyparser": "^4.3.0"
            <% } %>
              }
              }