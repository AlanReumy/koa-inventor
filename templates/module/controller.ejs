import <%= (moduleName) %>Service from '../service/<%= (moduleName) %>.service.js' 

class <%=(moduleName).slice(0,1).toUpperCase() + moduleName.slice(1) %>Controller {}

export default new <%=((moduleName).slice(0,1).toUpperCase() + moduleName.slice(1)) %>Controller()